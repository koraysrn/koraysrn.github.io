import type { ReactNode } from 'react';

export const devices: { name: string; icon: ReactNode }[] = [];

export const webExtensions: { name: string; href: string }[] = [];

export const software: { name: string; href: string }[] = [];
